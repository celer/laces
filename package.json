{
  "name": "laces",
  "version": "0.0.1",
  "description": "A client side / server side utility to generate forms and menus for bootstrap from a JS object",
  "main": "laces.min.js",
  "keywords": [
    "bootstrap",
    "html"
  ],
	"scripts":{
		"prepublish":"make clean all"
	},
  "dependencies": {
    "fire-ts": "0.0.4",
    "uglify-js": "~2.2.5"
  }
}
